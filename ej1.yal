rule tokens =
	[' ' '\t' '\n']		{ tokenize lexbuf }  (* ignorar espacios en blanco *)
  |  ['0'-'9']+				{ INT (int_of_string num) }
  | '+'								{ PLUS }
  | '-'								{ MINUS }
  | '*'								{ TIMES }
  | '/'								{ DIVIDE }
